name: Checkout Example

on: 
  workflow_dispatch:

jobs: 
  build:
    runs-on: ubuntu-latest
    steps: 
       - name: Checkout
         uses: actions/checkout@v5.0.0
         with: 
           repository: kowakawo/calculator-app-finished
       - name: Show listing and contents of directories
         run: ls -alhR
         
         
